# Prefer the repo-root `.env` for secrets + DB.
# Vite only reads this Frontend/.env for VITE_* (run `make env` to create it).
VITE_SERVER_URL=http://localhost:8080
VITE_CLIENT_URL=http://localhost:5173
